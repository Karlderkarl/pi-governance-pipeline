---
description: Set up or re-pin the auto-develop pipeline from existing governance (init + dry-run)
argument-hint: "[--harness <spec>] [--local] [--force]"
---
Load the `governance-pipeline` skill and follow **Mode: automate** for this repository.

Use `node <package>/bin/pipeline.mjs init ${ARGUMENTS:-}` for the init step.
